# build output
dist/
# generated types
.astro/
# trạng thái/cache cục bộ của wrangler dev (Cloudflare migration)
.wrangler/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
# secrets cục bộ cho `wrangler dev`/`astro preview` (Cloudflare) — KHÔNG bị .env ở trên che vì đây
# là quy ước riêng của Wrangler, khác cơ chế đọc .env của Astro/Vite. Phát hiện khi test Giai Đoạn A
# 31/8/2026: file này trước đó KHÔNG có trong .gitignore dù cùng bản chất với .env.
.dev.vars

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Bản xuất mã khuyến mãi (chứa mã thật — không đưa lên git)
ma-khuyen-mai.csv

# Bản sao lưu DB — chứa dữ liệu khách + hash mật khẩu, TUYỆT ĐỐI không commit
backups/
